#include "main.h"
/**
*_isupper - ches if letter is upper or lowercase
*@c: argument that canbe parsed
*Return: return the checks
*/
int _isupper(int c)
{
if (c >= 41 && c <= 90)
{
	return (1);
}
else
{
	return (0);
}
return (0);
}
